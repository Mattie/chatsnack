# Generated assets 0.7.0

## Goal

A continued `Chat` keeps generated images and files as local, reusable assets.

## Goal tests

- [x] G1: generate an image, save the chat, reload it offline, and read the same bytes
- [x] G2: capture a code interpreter file before `.chat()` returns
- [x] G3: pass a generated `ChatFile` into a later chat

## Work

- [x] Store generated bytes under `datafiles/chatsnack/assets/sha256/`
- [x] Return `ChatFile` values from `.images` and `.files`
- [x] Save provider-neutral `asset` references in YAML
- [x] Keep old `path`, `url`, and `file_id` references loadable
- [x] Prove image and code interpreter output with gated live tests
- [x] Add terse notebook examples for `.images` and `.files`
- [x] Set the package version to 0.7.0
